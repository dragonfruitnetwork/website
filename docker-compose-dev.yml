services:
  mariadb:
    container_name: mariadb
    image: mariadb:11.6.2
    ports:
      - "3306:3306"
    environment:
      MARIADB_ROOT_PASSWORD: dragonfruitnet
      MARIADB_DATABASE: dragonfruit
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data: {}